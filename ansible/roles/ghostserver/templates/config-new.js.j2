{
    "url": "{{ item.ghost_url }}",

    "referrerPolicy": "origin-when-cross-origin",

    "mail": {
      "transport": "Direct"
    },

    "database": {
        "client": "mysql",
        "connection": {
            "host": "localhost",
            "user": "{{ item.ghost_mysql_user}}",
            "password": "{{ item.ghost_mysql_password }}",
            "database": "{{ item.ghost_mysql_db}}",
            "charset": "utf8"
        },
        "debug": false
    },

    "fileStorage": true,

    "server": {
        "host": "0.0.0.0",
        "port": "{{ item.ghost_port }}"
    },

    "paths": {
        "contentPath": "{{ item.ghost_home }}/content"
    },

    "logging": {
      "path": "{{ item.ghost_log_dir }}",
      "level": "debug",
      "rotation": {
        "enabled": true,
        "count": 15,
        "period": "1d"
      },
      "transports": ["stdout", "file"]
    }
}
